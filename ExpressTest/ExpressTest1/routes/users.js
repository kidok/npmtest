var router = require('./base.js');
debugger
var express = require('express');
//var router = express.Router();


/* GET users listing. */
router.get('/', function(req, res, next) {
  res.send('respond with a resource');
  var aa = 444;
  debugger
});

module.exports = router;
